const { asscreate, assEdit, statusUpdate, assGetAll, assDelete, asscrDropDown } = require('./assessor')

module.exports = {
    asscreate,
    assGetAll,
    assEdit,
    statusUpdate,
    assDelete,
    asscrDropDown

}