const {add_batch ,batch_bulkupload, moveBatch, deleteBatch, bulkDataList, getBatchData, editBatchData, deleteBatchData, scheduleBatchList, currentBatch,dropDown} = require('./batch')

module.exports= {
    add_batch,
    batch_bulkupload,
    moveBatch,
    deleteBatch,
    bulkDataList,
    getBatchData,
    editBatchData,
    deleteBatchData,
    scheduleBatchList,
    currentBatch,
    dropDown  

}