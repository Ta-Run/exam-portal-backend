const {questionCreate,questionList,questionRemove, questionEdit, questionStatusChange, questionBulkUpload, getQuestionBulkUpload} =require('./question')

module.exports={
  questionCreate,
  questionList,
  questionRemove,
  questionStatusChange,
  questionEdit,
  questionBulkUpload,
  getQuestionBulkUpload,
}